"""
Django application configuration for requestutils application
"""
from django.apps import AppConfig


class RequestutilsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "requestutils"
